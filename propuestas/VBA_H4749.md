# #VBA #H4749 Obtener subcuenta de ventas de la ficha de cliente si está informada como se hace en proveedores

Los objetivos de este proyecto son:

+ Poder indicar una subcuenta de ventas en la ficha del cliente de forma que, si está informada, se use dicha subcuenta en lugar de la que se obtiene actualmente.

## Propuesta realizada a petición de:
Ramona

## Estructura

### Clientes
Añadiremos a la pantalla un campo en tabla / formulario de subcuenta de ventas (ver campo de subcuenta de compras en clientes y hacer igual)

### Asiento de factura de compra. Refactorización
Para evitar usar la variable global _codProveedorCompra_ en flfacturac, refactorizar _vbarba_generarPartidasCompra_ en algo así:
```js
function vbarba_generarPartidasCompra(curFactura, idAsiento, valoresDefecto : Array, concepto)
{
	const _i = this.iface;

    const datosCuentaCompra = _i.getDatosCuentaCompraProveedor(curFactura.valueBuffer("codproveedor"));

    if (!datosCuentaCompra) {
        return _i.__generarPartidasCompra(curFactura, idAsiento, valoresDefecto, concepto);    
    }
    return datosCuentaCompra;

    // ANTES
	// _i.codProveedorCompra = curFactura.valueBuffer("codproveedor");
	// return _i.__generarPartidasCompra(curFactura, idAsiento, valoresDefecto, concepto);
}
```
getDatosCuentaCompraProveedor debe buscar la subcuenta en la ficha y si la factura no tiene codproveedor o la ficha no tiene subcuenta, devolver False.

Luego hay que quitar la función _vbarba_datosCtaEspecial_ y la variable global .

### Asiento de factura de venta
Crear las mismas funciones que para el de factura de compra.

